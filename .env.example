# Copy to .env and fill in.  .env is gitignored — never commit the real key.
#
#   cp .env.example .env
#
# Get a key at https://platform.openai.com/api-keys
# Permit Chat Completions for the project's allowlisted models. Embeddings are NOT
# needed (SBERT/CLIP run locally). Keep auto-reload off; project code also stops at
# its conservative $8 ledger ceiling.
OPENAI_API_KEY=sk-proj-...
